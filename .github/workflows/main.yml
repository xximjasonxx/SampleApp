# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Docker Approve

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: 697ff755-4e3d-4c2c-988e-c28d196c12f2
          tenant-id: e15b8f94-b657-4f98-a1f2-9df013c37c73
          subscription-id: cd35503e-9e28-4b2b-8445-45fc816ba088